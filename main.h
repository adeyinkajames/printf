#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

int _printf(const char *format, ...);
int putcharj(char c);
int putsj(char *c);
int putcharj_c(char c);

#endif
